#include <bits/stdc++.h>

using namespace std;
using ll = long long;
using vi = vector<int>;
using vll = vector<ll>;
#define f(i,start,n) for (int i=start;i<n;i++)
#define all(a) (a).begin(), (a).end()
void printarr(vi v) {
    f(i,0,v.size()) {
        cout << i << "=" << v[i] << " ";
    }
    cout << endl;
}

void solve() {
    int n;
    cin >> n;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int t;
    cin >> t;
    while (t--)
    solve();
}
